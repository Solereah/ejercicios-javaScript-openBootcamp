const nombre = 'Sol'
const apellido = 'Lereah'
const estudiante = nombre.concat(' ', apellido)
const estudianteMayus = estudiante.toUpperCase()
const estudianteMinus = estudiante.toLocaleLowerCase()
const numeroLetras = estudiante.length
const primeraLetra = nombre[0]
const ultimaLetra = apellido.slice(-1)
const cadenaSinEspacios = estudiante.replace(' ', '')
const incluyeNombre = estudiante.includes(nombre) 

